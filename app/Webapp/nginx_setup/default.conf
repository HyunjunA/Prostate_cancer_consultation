# # Rate limiting zone (API protect)
# limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;

# server {
#   listen 80;
#   server_name _;

#   # Upload size limit
#   client_max_body_size 16m;

#   # Security headers
#   add_header X-Content-Type-Options nosniff always;
#   add_header X-Frame-Options DENY always;
#   add_header X-XSS-Protection "1; mode=block" always;
#   add_header Referrer-Policy "strict-origin-when-cross-origin" always;

#   # Hide Nginx version
#   server_tokens off;

#   # Nginx health check
#   location /nginx-health {
#     access_log off;
#     return 200 "healthy\n";
#     add_header Content-Type text/plain;
#   }

#   # Backend API
#   location /api/ {
#     # Rate limiting (burst up to 20 requests)
#     limit_req zone=api_limit burst=20 nodelay;
    
#     proxy_pass         http://prostatecancer-backend:8000;
#     proxy_http_version 1.1;
    
#     # Proxy headers
#     proxy_set_header   Host $host;
#     proxy_set_header   X-Real-IP $remote_addr;
#     proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header   X-Forwarded-Proto $scheme;
    
#     # Timeouts
#     proxy_connect_timeout 60s;
#     proxy_send_timeout 60s;
#     proxy_read_timeout 60s;
    
#     # Hide backend server info
#     proxy_hide_header X-Powered-By;
#   }

#   # Frontend (Next.js)
#   location / {
#     proxy_pass         http://prostatecancer-webapp:3000;
#     proxy_http_version 1.1;
    
#     # Proxy headers
#     proxy_set_header   Host $host;
#     proxy_set_header   X-Real-IP $remote_addr;
#     proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header   X-Forwarded-Proto $scheme;
    
#     # WebSocket support (Next.js hot reload etc)
#     proxy_set_header   Upgrade $http_upgrade;
#     proxy_set_header   Connection "upgrade";
#     proxy_cache_bypass $http_upgrade;
    
#     # Timeouts
#     proxy_connect_timeout 60s;
#     proxy_send_timeout 60s;
#     proxy_read_timeout 60s;
#   }

#   # Static files caching (optimal for Next.js static assets)
#   location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
#     proxy_pass http://prostatecancer-webapp:3000;
#     expires 7d;
#     add_header Cache-Control "public, max-age=604800, immutable";
#   }

#   # Deny access to hidden files
#   location ~ /\. {
#     deny all;
#     access_log off;
#     log_not_found off;
#   }
# }




# 
# ../Webapp/nginx_setup/default.conf

server {
  listen 80;
  server_name _;

  # Optional: increase size limit for uploads
  client_max_body_size 16m;

  # Frontend (Next.js)
  location / {
    proxy_pass         http://prostatecancer-webapp:3000;
    proxy_http_version 1.1;
    proxy_set_header   Host $host;
    proxy_set_header   X-Real-IP $remote_addr;
    proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header   X-Forwarded-Proto $scheme;
    proxy_set_header   Upgrade $http_upgrade;
    proxy_set_header   Connection "upgrade";
  }

  # Backend API
  location /api/ {
    proxy_pass         http://prostatecancer-backend:8000;
    proxy_http_version 1.1;
    proxy_set_header   Host $host;
    proxy_set_header   X-Real-IP $remote_addr;
    proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header   X-Forwarded-Proto $scheme;
  }

  # Nginx health
  location /nginx-health {
    return 200 "ok";
    add_header Content-Type text/plain;
  }
}



# trial 2
# # Rate limiting zone (API protect)
# limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;

# server {
#   listen 3000;
#   server_name _;

#   # Upload size limit
#   client_max_body_size 16m;

#   # Security headers
#   add_header X-Content-Type-Options nosniff always;
#   add_header X-Frame-Options DENY always;
#   add_header X-XSS-Protection "1; mode=block" always;
#   add_header Referrer-Policy "strict-origin-when-cross-origin" always;

#   # Hide Nginx version
#   server_tokens off;

#   # Nginx health check
#   location /nginx-health {
#     access_log off;
#     return 200 "healthy\n";
#     add_header Content-Type text/plain;
#   }

#   # Backend API
#   location /api/ {
#     # Rate limiting (burst up to 20 requests)
#     limit_req zone=api_limit burst=20 nodelay;
    
#     proxy_pass         http://prostatecancer-backend:8000;
#     proxy_http_version 1.1;
    
#     # Proxy headers
#     proxy_set_header   Host $host;
#     proxy_set_header   X-Real-IP $remote_addr;
#     proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header   X-Forwarded-Proto $scheme;
    
#     # Timeouts
#     proxy_connect_timeout 60s;
#     proxy_send_timeout 60s;
#     proxy_read_timeout 60s;
    
#     # Hide backend server info
#     proxy_hide_header X-Powered-By;
#   }

#   # Frontend (Next.js)
#   location / {
#     proxy_pass         http://prostatecancer-webapp:3000;
#     proxy_http_version 1.1;
    
#     # Proxy headers
#     proxy_set_header   Host $host;
#     proxy_set_header   X-Real-IP $remote_addr;
#     proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header   X-Forwarded-Proto $scheme;
    
#     # WebSocket support (Next.js hot reload etc)
#     proxy_set_header   Upgrade $http_upgrade;
#     proxy_set_header   Connection "upgrade";
#     proxy_cache_bypass $http_upgrade;
    
#     # Timeouts
#     proxy_connect_timeout 60s;
#     proxy_send_timeout 60s;
#     proxy_read_timeout 60s;
#   }

#   # Static files caching (optimal for Next.js static assets)
#   location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
#     proxy_pass http://prostatecancer-webapp:3000;
#     expires 7d;
#     add_header Cache-Control "public, max-age=604800, immutable";
#   }

#   # Deny access to hidden files
#   location ~ /\. {
#     deny all;
#     access_log off;
#     log_not_found off;
#   }
# }