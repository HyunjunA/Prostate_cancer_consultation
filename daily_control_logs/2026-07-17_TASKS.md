# 2026-07-17 (Fri) — Tasks

> Names are replaced with role labels per repository convention. The support email address
> added to the UI is a functional requirement and is deliberately not reproduced here.

## Coordinator request — add a support contact to the survey completion screen ✅ done

**Request (by email)**: add the support team's email address after "our support team"
on the follow-up visit report, so participants know where to ask questions.

- before: `... please contact your healthcare provider or our support team.`
- after: `... please contact your healthcare provider or our support team at <support email>.`

**Change**: `app/Webapp/src/components/PatientFollowUpReportV38.tsx` (line 614)

- Location: the `CompleteStep` component — the completion / thank-you screen shown after
  the whole survey is submitted

**Deployment**: webapp Docker image rebuilt

- `docker compose -f docker-compose-frontend.yml build webapp` → container replaced
- Verified: the new copy appears in the `.next` build output inside the running
  container (`prostatecancer-webapp-native`); **zero** occurrences of the old
  `our support team.` string; `:3001` responds normally

### Worth noting

- The only follow-up component actually in use is **V38**. `V31`, `V31Re`, and
  `ModifiedV31` contain the same string but are all dead files (commented out in
  `page.tsx`, imported nowhere), so they were left alone.
- **The first-visit report (`PatientInitialVisitReportV42.tsx`) has no "our support
  team" phrasing at all** — only `please contact your healthcare provider.`
  → Participants see that screen too, so **the coordinator should be asked whether the
  contact belongs there as well**.
- ⚠️ Putting a real email address in source code is an **exception** to the
  no-real-names rule — it is a functional requirement (a participant
  contact point) and was made with the user's approval.

### Open / follow-up

- ⬜ This change is **not committed** — it exists only locally and in the container, not
  in git. A decision on whether to commit is needed.
- ⬜ Reply to the coordinator: should the contact also appear on the first-visit report?
