# 2026-05-07 — Tasks

Two threads today, captured in one log:

1. (morning, completed) V37 per-domain Submit buttons + backend persistence of the 14 experimental-arm inputs.
2. (mid-morning, partially done — rest queued for afternoon) Email from [manager] requesting REDCap survey access verification and a manual end-to-end test with three patients before a 1 PM sync.

The "no password" instruction in the email confirms the team-level decision recorded earlier today to drop the patient-login feature.

---

## 0. Sources

### Email from [manager] — to Jun (1:1)

> Good morning Jun. Please first get access to the survey to see the changes in REDCap. That is priority and then end to end testing, not automatic, manual is OK, creating 3 users from dropping the conversation, running the pipeline, using the patient interface, making sure it saves all data back from the interface, answering the survey and checking it is saved in REDCap. Also checking the doctor's dashboard with the created patients. We can meet after 1 PM to see where we are at, we are not going to use password.

### Mapping to current state

| [manager] ask | Status |
|---|---|
| First priority — get access to survey, see changes in REDCap | ✅ Done. Export rights enabled in REDCap UI mid-morning; metadata snapshot saved at `playground/Redcap_api_playground/metadata_snapshot_2026-05-07.json`; webapp ↔ REDCap question texts compared — 25 of 25 questions match (DCS Q8 trailing period is the only micro-diff). |
| Manual E2E with **3 users** — drop conversation → run pipeline → patient interface → save back → survey → REDCap → doctor dashboard | ⏳ Outstanding — see §4. |
| Meeting after 1 PM | Time-bound. |
| "Not going to use password" | ✅ Aligns with the team decision to drop the login feature, also recorded today. |

---

## 1. Status overview

### Done today

- [x] V37 per-domain Submit buttons + Submission Progress indicator (frontend-only), webapp Docker rebuilt + redeployed, browser-verified for `le` / `ed` / `ius` domains.
- [x] V37 backend persistence — migration 010, `patient_first_visit_responses` table, GET / PUT routes, Pydantic factor whitelist, Jest hook tests, integration tests (27 / 27 + 553 / 553 regression), end-to-end PUT/GET via webapp proxy verified.
- [x] Branch `feat/v37-first-visit-persistence` committed and pushed; PR #9 opened.
- [x] REDCap export-rights granted (manager priority 1); metadata snapshot saved; webapp ↔ REDCap survey question diff produced.
- [x] Login feature dropped per team decision; orphan files preserved without commit.

### Outstanding — priority order

1. **🔴 TOP PRIORITY — Post Risk Perception: REDCap form vs follow-up page mapping verification** — see §3.4. REDCap project has THREE risk-related forms (`pre_risk_perception` 5 fields / `post_risk_perception` **0 fields, empty** / `risk_perception` 5 fields). Our follow-up page + backend currently writes only to the legacy `risk_perception` form. If REDCap admin intends `post_risk_perception` to be the canonical destination for post-consultation answers, every submission today is landing in the wrong place. Confirm with [manager] / REDCap admin first; update `routes_surveys.py` and `Frontend_REDCap_Field_Mapping.md` after the answer.
2. **Manual E2E test with 3 users** ([manager] priority 2) — see §4 for the 7-step flow and verification SQL.
3. **Confirm SID for the third patient** — only SID 10 (already processed) and SID 14 (`prostate_cancer_R01_Guille/Input_Keystrokes REC001 (SID 14).xlsx`) are easy to find; SID 15 input transcript location is unknown — needs a quick answer from [manager] or the data folder owner before the test can target three distinct patients.
4. **Doctor dashboard pass** with the three patients (URL + checklist in §4 step 6).
5. **Notes for the 1 PM sync** — short bullet of what passed / what blocked.

### Coordination

- [manager] is in another deliverable today — async by default. The 1 PM sync is the next live touchpoint.
- "No password" decision is now redundant with the dropped login feature; nothing further to do.
- REDCap export rights persist for the project, so the comparison script can be re-run on demand.

---

## 2. V37 persistence — what shipped (PR #9)

### 2.1 Frontend — per-domain Submit buttons + working Submission Progress

Single file modified for the visible UX change: `app/Webapp/src/components/PatientInitialVisitReportV37.tsx`.

| Change | Where |
|---|---|
| Renamed indicator label "Rating Progress" → **"Submission Progress"** | Indicator block near the top of the report |
| Renamed `ratingProgress` → `submissionProgress`; replaced the dead `ratings[topic] > 0` count with a new `submittedDomains: Record<string, boolean>` count | useMemo in main component |
| Added `submittedDomains` state + `handleSubmitDomain(topic)` handler | After existing `ratings` state / handlers |
| Added per-domain Submit button at the bottom of each `TopicCard` (5 buttons total) | Inside TopicCard JSX, just below the Evidence section |
| Re-submit affordance | Button label switches "Submit" → "Submitted ✓ — Click to update". Re-clicking is a no-op for the count (5/5 is preserved). |
| Completion message wording | "Thank you for **submitting** all topics!" (was "rating") |

### 2.2 V37 backend persistence

| File | Action |
|---|---|
| `app/Backend/migrations/versions/010_add_patient_first_visit_responses.py` | New table — one row per `(file, speaker, domain)`; ten columns; CHECK on domain enum + VAS range; FK CASCADE to `patient_summary`. |
| `app/Backend/models.py` | New `PatientFirstVisitResponses` ORM model. |
| `app/Backend/routes_patient.py` | New `GET /api/patient/first-visit-responses/{file}/{speaker}` and `PUT /api/patient/first-visit-responses` (upsert). Pydantic factor whitelist mirrors the V37 source arrays. Auth follows existing `update_patient_scoring` pattern. |
| `app/Backend/tests/integration/test_first_visit_responses.py` | 27 cases: round-trip, partial PUT, 401 / 403 / 422, CASCADE, alembic up/down. |
| `app/Webapp/src/api/firstVisitApi.ts` | New typed fetch wrapper through the `/api/backend` proxy. |
| `app/Webapp/src/hooks/useFirstVisitResponses.tsx` | New hook — mount-time GET prefill + `saveDomain()` + `isHydrated` race guard. |
| `app/Webapp/src/components/PatientInitialVisitReportV37.tsx` (further edit) | Hook consumer — Submit click PUTs first and only flips `submittedDomains[topic]` on a 200; mount-time prefill of the 14 useState slots. |
| `app/Webapp/src/__tests__/hooks/useFirstVisitResponses.test.ts` | Five Jest cases. |
| `dev_docs/V37_First_Visit_Persistence_Design.md` (+ `_KR.md`) | Design rationale: schema, FK choice, intentionally-omitted columns, upsert pattern, hydration race, error handling. |
| `dev_docs/TODO.md` | P0-D-2 marked done with reference to this PR. |
| `daily_control_logs/2026-04-30_TASKS.md` | Gap A closed; Item 4 / "Login flow wiring" marked DROPPED per team decision. |

### 2.3 Hot-fix during deployment

The webapp container was first started with `API_KEY` empty in the shell environment that ran `docker compose up`, which made all backend calls return `403 Forbidden`. Resolved by re-running the recreate after `export API_KEY=$(grep '^API_KEY=' app/Backend/.env.native | cut -d= -f2)`. A second rebuild was needed after a stale `firstVisitApi.ts` URL (`/api/backend/api/...`) was corrected to `/api/backend/...`.

### 2.4 Verification at end of morning

- DB rows confirmed for 3 of 5 domains (le, ed, ius) submitted by the user via the browser; cp had a curl-test row from earlier.
- Backend log shows `PUT /api/patient/first-visit-responses` returning 200 for each Submit click.
- Pre-push hook ran 630 collected tests successfully before push.

---

## 3. REDCap survey access — what was verified

### 3.1 Permission unblocked

The R01 NLP-RiskFeedback API key initially returned `403 "No Access" data export rights`. After the user enabled `Data Exports → Full Data Set` for the same user in the REDCap UI ("User Rights" page) the export endpoints started returning 200. Both the playground key and the backend `REDCAP_API_TOKEN` belong to the same user; the permission change applied to both.

### 3.2 Snapshot

Saved at `playground/Redcap_api_playground/metadata_snapshot_2026-05-07.json` (60 KB JSON, full project metadata). Future drift checks can `diff` against this baseline.

### 3.3 Question diff webapp ↔ REDCap

| Survey | webapp source | REDCap form | Texts match? |
|---|---|---|---|
| DCS — 16 items | `app/Webapp/src/components/surveysSecondVersion/DecisionalConflictSurvey.tsx` | `decisional_conflict_survey` | ✅ except DCS Q8 trailing period: webapp `"...without pressure from others."` vs REDCap `"...without pressure from others"` (no period). Cosmetic. |
| SDM — 4 items | `surveysSecondVersion/SDMQuestions.tsx` | `shared_decision_making_sdm` | ✅ exact, including the `[intervention]` placeholder shared by both sides. |
| Risk Perception — 5 items (1 slider + 4 radio) | `surveysSecondVersion/RiskPerceptionSurvey.tsx` | `risk_perception` | ✅ exact, including all option labels on Q2–Q5. Q1 webapp uses 0–100 slider with `sliderToCategory()` mapping 0–7→1, 8–14→2, 15–24→3, 25–34→4, 35+→5. |

### 3.4 ⚠️ Post Risk Perception — verification required

The REDCap project's metadata snapshot taken today shows **three** risk-related forms coexisting:

| REDCap form name | Field count | Status |
|---|---|---|
| `pre_risk_perception` | 5 | populated, presumably for pre-consultation capture |
| `post_risk_perception` | **0** | **empty form** — exists but no fields defined yet |
| `risk_perception` | 5 | the legacy form our backend has always written to |

**Current behaviour of our follow-up page**: `app/Webapp/src/components/surveysSecondVersion/RiskPerceptionSurvey.tsx` (5 questions: cancer untreated VAS + 4 multiple-choice) → `submitSurvey({survey_type: 'risk_perception', ...})` → backend `routes_surveys.py` POSTs to REDCap form **`risk_perception`** (the legacy one), and **never touches `post_risk_perception`**.

**Why this matters before the next REDCap analysis pass**:
- If `post_risk_perception` is the form REDCap admin intends to be canonical for post-consultation responses (the new form was created for a reason), every submission we make today is landing in the wrong place from REDCap's perspective.
- If `risk_perception` (legacy) is still canonical and `post_risk_perception` is just a stub, no action is needed beyond confirming.
- Either way the answer determines whether `routes_surveys.py` and `Frontend_REDCap_Field_Mapping.md` need updating.

**Action items for the next [manager] sync / REDCap admin contact**:

1. **Confirm canonical form** — ask [manager] or the REDCap project owner which of the three risk forms holds post-consultation answers going forward.
2. **If `post_risk_perception` is canonical**:
   - Get the field definitions populated on the REDCap side (currently 0 fields).
   - Update `app/Backend/routes_surveys.py` to POST to `post_risk_perception` instead of `risk_perception` for the survey_type `risk_perception`.
   - Refresh `docs/features/Frontend_REDCap_Field_Mapping.md` and `REDCap_Instruments_Fields_Documentation.md` to point at `post_risk_perception`.
   - Add an Alembic migration only if any backend column references the form name (currently none — survey form is a string, not a column constraint).
   - Re-run the follow-up Playwright spec to confirm the new mapping round-trips.
3. **If `risk_perception` (legacy) stays canonical**: document the empty `post_risk_perception` form as deprecated/unused so it doesn't trip up future readers; no code change needed.
4. **Either way**: capture the decision in `dev_docs/V37_First_Visit_Persistence_Design.md` (it already mentions this anomaly briefly in §1, but should be promoted to a closed decision once the answer lands).

The current `playground/Redcap_api_playground/metadata_snapshot_2026-05-07.json` snapshot is the baseline to compare against once the REDCap admin finishes whatever edit they were mid-stream on. Re-run `playground/Redcap_api_playground/1.R01_NLP_RiskFeedback_view_record_ids.py` after the change to confirm `post_risk_perception` either acquires fields or disappears.

---

## 4. Manual E2E test plan ([manager] priority 2)

### 4.1 Available transcripts

| SID | Input file | Status |
|---|---|---|
| 10 | `/Users/choih2/Documents/GitHub/Graciela_Lab_Collab/prostate_cancer_project/AI_physician_patient_communication/data/input/Input_Keystrokes REC 001 (SID 10).xlsx` | Already processed — `analysis_id=1` exists |
| 14 | `/Users/choih2/Documents/GitHub/Graciela_Lab_Collab/prostate_cancer_project/prostate_cancer_R01_Guille/Input_Keystrokes REC001 (SID 14).xlsx` | Available — copy into `data/input/` and run pipeline |
| **3rd patient (TBD)** | — | ⚠️ Need confirmation — see §1 outstanding item 2 |

### 4.2 Per-patient flow (repeat for each of the three SIDs)

```
1. Drop conversation transcript
   cp <SID xlsx> /Users/choih2/Documents/GitHub/Graciela_Lab_Collab/prostate_cancer_project/AI_physician_patient_communication/data/input/

2. Run pipeline
   /Users/choih2/Documents/GitHub/Graciela_Lab_Collab/prostate_cancer_project/Prostate_cancer_consultation_dashboard/.venv/bin/python \
     /Users/choih2/Documents/GitHub/Graciela_Lab_Collab/prostate_cancer_project/Prostate_cancer_consultation_dashboard/scripts/run-pipeline-standalone.py \
     --file <SID xlsx>
   Verify: new transcript_analysis_log row + 5 patient_summary_domain rows + 5 llm_domain_scoring_and_summary rows.

3. Patient first visit (browser, no password — query string only)
   http://localhost:3001/?fileid=<file>&patid=<speaker>
   Fill the 14 V37 inputs across cp/le/ed/inc/ius. Click Submit on each card.
   Verify: 5 rows per patient in patient_first_visit_responses; Submission Progress 5/5.

4. Patient follow-up (same patient)
   Fill DCS (16) + SDM (4) + Risk Perception (5 = 1 slider + 4 radio) + Patient Satisfaction (1) = 26 inputs.
   Click each survey's Submit.
   Verify: 4 rows per patient in survey_submission_log, all with redcap_synced = true.

5. REDCap verification
   cd /Users/choih2/Documents/GitHub/Graciela_Lab_Collab/prostate_cancer_project/Prostate_cancer_consultation_dashboard/playground/Redcap_api_playground
   /Users/choih2/Documents/GitHub/Graciela_Lab_Collab/prostate_cancer_project/Prostate_cancer_consultation_dashboard/.venv/bin/python 1.R01_NLP_RiskFeedback_view_record_ids.py
   Verify: the patient's record_id appears with ✓ for DCS / SDM / Risk / Satisfaction.

6. Doctor dashboard
   http://localhost:3001/?doctorid=<doctor>
   Verify: all three patients listed; score trajectory chart populated; sentence rewrite UI loads.

7. Move on to the next patient.
```

### 4.3 End-of-test verification SQL (after all three patients)

```sql
SELECT COUNT(*) AS analyses FROM transcript_analysis_log;
-- expect: 3 (or 1 prior + 2 new)

SELECT COUNT(DISTINCT speaker) AS distinct_patients FROM patient_summary;
-- expect: 3

SELECT speaker, COUNT(*) AS domain_rows
FROM patient_first_visit_responses GROUP BY speaker;
-- expect: 5 per speaker

SELECT speaker, COUNT(*) AS surveys
FROM survey_submission_log GROUP BY speaker;
-- expect: 4 per speaker

SELECT speaker, COUNT(*) AS synced
FROM survey_submission_log
WHERE redcap_synced = true GROUP BY speaker;
-- expect: 4 per speaker (every survey makes it to REDCap)
```

### 4.4 What I (Claude) can do automatically vs what needs you

| Automatable | Manual (you) |
|---|---|
| Steps 1, 2 (transcript copy + pipeline) | Steps 3, 4, 6 (browser interaction) |
| Step 5 partial (re-running the view-record-ids script) | Step 7 (decide to continue / stop) |
| Step 4.3 SQL verification | Watching the UI for visual regressions |

I will run the automatable steps end-to-end while you do the browser parts.

---

## 5. Login feature — discontinued per team decision

The patient-login UI work scaffolded earlier (`feat/login-ui-component` branch + `app/Webapp/src/app/login/page.tsx` + `app/Webapp/src/components/LoginCard.tsx`) is dropped. Files and branch are preserved as historical artifact — no further auth wiring will be proposed for the patient-facing flow. The "no password" line in [manager]'s email today reaffirms this. Item 4 ("Patient password scheme") and the "Login flow wiring" follow-on in `daily_control_logs/2026-04-30_TASKS.md` are now closed without action.

---

## 6. Risks tracked

| Risk | Mitigation |
|---|---|
| Third SID input transcript not located in time → can only test two distinct patients | Ask [manager] in advance of 1 PM; alternatively run SID 10 a second time as a re-submission test (covers the upsert path but not the new-record path). |
| Webapp container's `API_KEY` env unset on next recreate → 403 trap returns | Always run `export API_KEY=$(grep '^API_KEY=' app/Backend/.env.native | cut -d= -f2)` before `docker compose up -d webapp`. |
| REDCap export permissions revert (admin housekeeping) | Snapshot already saved at `playground/Redcap_api_playground/metadata_snapshot_2026-05-07.json`; can re-grant from User Rights page if it happens. |
| `risk_perception` vs new empty `post_risk_perception` form mismatch | Out of scope today; flag for [manager] at 1 PM. |
| Pipeline timing per patient (~4 minutes for SID 10) means 3 patients ≈ 12 minutes plus the manual UI steps | Start the SID 14 pipeline as soon as the third SID is confirmed; run UI steps in parallel where possible. |

---

## 7. Branch + working tree at end of morning

- Branch: `feat/v37-first-visit-persistence` — pushed to `origin`, PR #9 open.
- Modified, intended for V37 PR (already committed): the 12 files listed in §2.2.
- Untracked, **left alone** (not part of any PR):
  - `app/Webapp/src/app/login/`, `app/Webapp/src/components/LoginCard.tsx` (login — dropped).
  - `dev_docs/Application_stack.txt` (EIS handout, separate concern).
- Gitignored (KR companions exist on disk, not in git):
  - `daily_control_logs/2026-05-07_TASKS_KR.md`
  - `dev_docs/V37_First_Visit_Persistence_Design_KR.md`
