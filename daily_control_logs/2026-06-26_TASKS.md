# 2026-06-26 — Tasks

English companion to `2026-06-26_TASKS_KR.md`.

Collaborators are referred to by role (per repo rule: no real names in tracked docs).

---

## 🔧 To-do — De-identification / Hashing Pipeline (confirmed 2026-06-26)

### 1. Implement the de-identification step — the core, the "only missing piece"
- New processing flow: **read input file → de-identify → hash the name (patient name)**
- Usage scenario: a **clinical team member** drops one or more files into a "de-identification screen" to process.
- The reversible SID hashing (reversible cipher) I already built is the **key component** of this de-id → fold it in as a **pipeline step**.

### 2. Produce two outputs
1. De-identified input files for the pipeline.
2. A **CSV mapping file** — columns: `real name` · `real patient ID` · `hashed ID` → so the clinical team can see the "hash ↔ real patient" mapping.

### 3. ⭐ Introduce Doctor ID — the biggest structural change
**Background:** the current assumption is "all patients belong to one doctor" (single-doctor dashboard). In reality there are multiple doctors → doctor separation is required.

**Implementation:**
- [ ] Pass a doctor ID + **hash the doctor ID**.
- [ ] Filename convention: `patientID_doctorID_date` (structure shared over chat).
- [ ] Add a **doctor ID column** to the DB schema/tables (some tables, not all).
- [ ] Dashboard reads the doctor ID from the **query string**.
- [ ] Final mapping table: `patient ID` · `real SID` · `hashed ID` · `doctor ID` · `hashed doctor ID` · `first (report) visit link` · `follow-up survey link` · `doctor link`.
- [ ] Keep doctor links simple — `doctor 1 / 2 / 3`.
- [ ] Behavior: read the doctor ID (hashed) → return **all data belonging to that doctor**.

### 4. Minimize query-string variables — "otherwise super dirty"
- [ ] Keep only strictly necessary query-string variables.
- [ ] Remove duplication: don't pass filename + patient ID + date all together.
- [ ] Keep: `patient ID` + `date` / Remove: full filename · prefix.

### 5. Pipeline side
- [ ] Pipeline reads the doctor ID and stores it in the updated DB schema.

### 6. Script / execution cleanup (pairing session, same day 14:36)
- [ ] Single `main` script that iterates the input folder to batch-process Excel files (no pasting filenames each time).
- [ ] Output the ID ↔ hash mapping as a CSV alongside.
- [ ] Add a two-way `hash ↔ SID` conversion function (numeric input).
- [ ] Upload processed files to OneDrive (input folder, etc.).
- [ ] Wipe all DB tables on the new deployment server, then re-run with the 11 inputs.
- [ ] Investigate and fix the new error that appeared at the end.
- [ ] Keep all notes on the discussed fixes (for continuation).

### 7. Subsequent order (after I finish implementation)
1. **Internal end-to-end test** (me + the deployment collaborator, refining details).
2. **Deploy to the actual server** (deployment handled on the collaborator's side — my credentials block some file processing; only DB hosting moves to server storage).
3. **Test with the clinical / NLP team**.

---

## ✅ Summary — my implementation checklist
1. De-id step (input → de-id → name hash).
2. CSV mapping output (real name · real ID · hashed ID).
3. Doctor ID: pass · hash / filename `patientID_doctorID_date` / schema column / dashboard query string / mapping · links.
4. Minimize query variables (patient ID + date only).
5. Pipeline stores the doctor ID.
6. Single script + conversion function + redeploy re-run + error fix.
