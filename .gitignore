notused

# Environment / secrets
.env
.env.*
!.env.example
!.env.*.example

# Python
__pycache__/
*.pyc
*.pyo

# Node
node_modules/
.next/

# OS
.DS_Store

# IDE
.vscode/
.idea/
.claude/

# Data files — NEVER commit
data/
*.csv
*.xlsx
*.xls

# Backend runtime data
app/Backend/logs/
logs/
app/Backend/uploads/
app/Backend/data/
Processed_Data_DB.csv

# Archive (all archive folders excluded from git)
archive/

# Korean-only document versions (local only, not pushed)
*_KR.md
*_KR.pdf
*_KR.sql
*_kr.txt
*_kr.sql
*_kr_summary.txt

# PDF build scratch dirs
docs/architecture/_build_pdf/

# Native deployment env files (host-local credentials, never committed)
*.native
.env.native

# Claude Code evaluation harness (local-only test/eval tooling)
.harness/

# Claude Code per-directory instruction files (local-only, machine-specific).
# Path-agnostic rule covers root + app/Backend/ + app/Webapp/ in one go.
CLAUDE.md

# Native deployment Python venv
.venv/

# Temporarily ignored 2026-05-08 — sibling clone at parent dir is the
# canonical source. Was previously a submodule (commit 7e9063b) but the
# duplication caused confusion and scripts only ever used the sibling
# path ($REPO_ROOT/../AI_physician_patient_communication).
# Re-evaluate when AI_physician_patient_communication path strategy is
# finalised.
AI_physician_patient_communication/
